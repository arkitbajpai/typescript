let drink ="chai"
// infer the data to string we have not decalare it.
 let cups =Math.random()>0.5 ? 10:5 

//  //the compiler's ability to automatically determine and assign types to variables, function returns, and expressions based on their assigned values and context 
// TypeScript is known as type infrencing

let name :string= "Arkit"  // type anottation we can overide it also.

